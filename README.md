# EVORON 电子书城

当前正式版本：**v6.11.5**。由 HistoryAI v6.11.5 生成。

## 本次修复

手机书籍详情页的阅读、书架状态和评分操作独立于封面右侧窄列，使用整行空间。
想读、在读、读完等宽排列，英文长标签可换行，避免横向溢出与按钮难以点中。
桌面保持封面在左、信息和操作在右的布局。

不修改工作台，不生成新封面，不改写正文，不更改发行门禁或用户阅读进度。

## 构建与验收

- 源模板来自引擎正式标签 v6.11.5，提交 `918cf041`。
- 构建基于书城 GitHub main `9811800ed0981809b0fd391b811b7b96f67cd248` 的隔离副本。
- 重建 206 本书详情页及 634 个历史发行详情页；总账 207 条、205 本公开书。
- 18,107 个受保护文件校验不变，包括章节正文、封面、发行元数据与当前发行指针。
- 15 个已有作者页面的姓名、简介保留；目录总账 SHA-256 不变。
- 发行完整性校验通过：205 本检查、70 本哈希清单验证、135 本兼容旧发行。
- 引擎全量测试 2503 通过、1 既有跳过、0 失败；lint/check 通过。
- 中英文详情页 320–1440px 共 16 组浏览器布局和书架/评分交互通过，接口使用隔离模拟。
- 未进行手机真机测试或生产用户账号写入。

历史记录 `proj-mtd449t0gwk` 缺少稳定发行元数据，本次维持原样，不凭空补造发行信息。
旧发行兼容告警未新增。本次不重建章节正文 HTML。

## 发布与回退

本仓库 main、注释标签和 GitHub Release 为书城真源。验收通过后推送 GitHub，
云端继续使用原 `sync-books.sh` 拉取，未修改自动更新规则，无需重启静态站。
回退应在 GitHub 新建回退提交后让云端拉取，避免撤销期间新增的发行记录。
